export default abstract class ClassEnum<T> {
    readonly value: string;
    constructor(value: string);
    static values<T>(): T[];
    private static getEnums;
    static valueOf<T>(value: string, defaultEnum?: T | null): T;
    name(): string;
    equals(e: ClassEnum<T>): boolean;
}
